<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.zerock.oscatest.mapper.RequesterMapper">

    <insert id="insert">
            insert into tbl_requester (reqID, reqName, reqCall, reqEmail,reqAddress, reqImg, reqBirth, reqPW)
            values (#{reqID}, #{reqName}, #{reqCall}, #{reqEmail}, #{reqAddress}, #{reqImg},#{reqBirth}, #{reqPW})
    </insert>



</mapper>